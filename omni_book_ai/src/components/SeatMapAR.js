import React from "react";

// PUBLIC_INTERFACE
export default function SeatMapAR() {
  /** Placeholder for AR seat map preview. */
  return (
    <div style={{
      padding: 32,
      background: "rgba(255,255,255,0.02)",
      border: "1px dashed var(--secondary)",
      borderRadius: 10,
      color: "var(--secondary)",
      minHeight: 80,
      textAlign: "center"
    }}>
      [Seat Map AR Placeholder]
    </div>
  );
}
