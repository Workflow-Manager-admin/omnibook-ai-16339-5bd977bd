import React from "react";

// PUBLIC_INTERFACE
function MultiLangProvider({ children }) {
  /** This is a stub for the MultiLangProvider context/provider. */
  return <>{children}</>;
}

export default MultiLangProvider;
