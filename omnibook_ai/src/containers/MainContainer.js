import React from "react";

// PUBLIC_INTERFACE
function MainContainer({ children }) {
  /** This is a stub for the MainContainer, serving as the app shell. */
  return <div className="main-container">{children}</div>;
}

export default MainContainer;
